# Week 36 (2026-08-31 → 2026-09-06)

## 2026-09-06 — Sajid's final points: calculator step, fee rounding, presence, signup

Branch `fix/client-final-points-2026-09-06`. Inputs: 3 annotated screenshots + `Currency.mp4` + 4 bullet asks.

- **Calculator step 500 for every currency** (`currency-config.ts`). Supersedes the 2026-09-04 ask of 10 000 for SEK/NOK/DKK/ISK — Sajid now wants 500 across the board. Test rewritten (all 7 currencies).
- **Fee rounding re-check:** `calculateClientFee` / `calculateRecruiterFee` were already nearest-10. Two stragglers fixed: the recruiter jobs-list fallback still floored to 100 with a stale `recruiter_fee_percentage ?? 7` (now `calculateRecruiterFee`, `floorToHundreds` deleted), and the admin fee overrides used `Math.round` (now `roundToTen`).
- **Presence "Not working":** prod is missing migration 080 (`presence_sessions` does not exist there), and `getOnlineCounts` swallowed the error into `0`. Now returns `null` → header pill shows "—" (verified locally by revoking the grant). **Applying 080 to prod is the actual fix** — Henrik.
- **Two recruiter texts reworded** (duplicate-candidate warning, salary-above-max note) in 4 dicts + component fallbacks.
- **Client signup error → three real defects found on the local stack:**
  1. **Data loss:** Supabase answers a signUp for an existing *unconfirmed* email by returning that same user; the `companies` insert then hit `companies_user_id_key` and the "cleanup" `deleteUser` **deleted the existing account** (cascade: profile + company). Fixed: rate-limited `profiles.email` pre-check, Supabase fake-user (`identities: []`) check, and a 23505 guard that never deletes. Reproducing vitest (`auth-register.test.ts`, 11 cases) written red first.
  2. **Password floor mismatch:** Supabase (`config.toml` + prod per Dev-Notes) requires 10 chars, the app validated 8 → Supabase rejected 8–9 char passwords as a generic error. App now 10 everywhere (zod, `minLength`, placeholders, reset page).
  3. **Unreadable errors:** all auth action errors were hardcoded Swedish (English UI) and a Supabase mail failure collapsed into "service unavailable". New pure `authErrorKey()` → dictionary keys (`emailAlreadyRegistered`, `confirmationEmailFailed`, `passwordRequirements`, …) in 4 dicts.
  Plus: company signup landed on `/company` → bounced to `/login` with no explanation → now `/register/company?submitted=1` "check your email" state; `<form action>` reset every field after an error → `onSubmit`; `/callback` added to the coming-soon PUBLIC_PATHS so the confirmation link works from a device without the preview cookie.
- **Account creation status (Sajid's question):** both `/register/company` and `/register/recruiter` work end-to-end (verified locally: pending row, confirmation mail in Mailpit, admin approval gate), but the whole site is behind the coming-soon gate (`COMING_SOON_ENABLED = true`) — only visitors with the preview link can reach them — and prod confirmation mail depends on Supabase Auth SMTP being configured (Resend), which is still open.

Verification: vitest 560 → 572 green, tsc/lint/build green, browser e2e on the local stack (calculator SEK/ISK/DKK step 500, EUR 100 500 → fee 11 060, both texts, signup success + duplicate path with account intact, presence "—"/0 paths). Security + code review by subagents: 0 Critical/High. Fixed from review: post-signUp mutations now require `isFreshUser` (created < 5 min ago) — closes the cross-role takeover variant and any non-23505 delete; register throttle 3/email+IP, 5/IP per 15 min (enumeration tradeoff recorded in [[2026-09-06-signup-enumeration-tradeoff]]); coming-soon public paths exact-or-child (`/callbackfoo` no longer bypasses); `/callback` uses `resolveRouteRole` and drops the `x-forwarded-host` redirect; password classifier narrowed; dependency advisories (sanitize-html, @tiptap/core, browserslist — reviewer confirmed NOT exploitable via our sanitizer config) DEFERRED: `npm audit fix --omit=dev` pruned devDependencies and bumped @tiptap/core out of sync with @tiptap/react (build red, dev server 500) → lockfile restored with `npm ci`. Do it as its own change: `npm update @tiptap/core @tiptap/react @tiptap/starter-kit sanitize-html browserslist`, then exercise the job-description editor. Deferred (pre-existing): zod form messages are hardcoded Swedish.

**2026-09-06 (later):** migrations 078/079/080 applied to prod by Henrik via `Dev-Notes/prod-apply-078-079-080.sql`; all post-checks true. Presence counter is live in prod.

## 2026-09-06 (later) — dependency bumps

Branch `chore/dependency-bumps-2026-09-06`, lockfile-only: all 28 `@tiptap/*` 3.22.3 → 3.31.3 together, sanitize-html 2.17.7, browserslist 4.28.9, `@humanfs/node` (dev) → `npm audit` 0 (prod and dev). Gate green (tsc/lint/574 tests/clean build). Editor verified headlessly with a one-off Playwright script driving the real job wizard on the local stack — the in-app Browser pane cannot finish framer-motion step transitions while hidden, and Claude-in-Chrome was not connected. A/B run: byte-identical editor HTML on old vs new tiptap, no console/page errors. Observation (pre-existing, harness-only so far): "select line with Shift+Home → click Bold → End/Enter" drops the selected line in headless Chromium on BOTH versions; likely `End` not collapsing the programmatically restored selection — worth a 30-second manual check in a real browser, not a blocker.
