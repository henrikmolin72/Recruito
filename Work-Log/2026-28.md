# Week 28 — 2026-07-06 → 2026-07-11

All shipped to `main` + prod unless noted. Client-feedback-driven week (annotated screenshots).

## Guarantee system
- Admin **Completed/Failed workflow** on placements + recruiter dashboard sync (`914da44`); PGRST201 embed-hint root fix.
- Guarantee now starts at the **client-confirmed joining date** — migration 067 (`61dfa0d`).
- Company dashboard guarantee **progress bars fixed** — `success-200/400` tokens were never defined in the Tailwind v4 palette, green-state bars rendered invisible (`1c1450e`).
- Recruiter payout copy: "released after Recruito receives client payment, ~2 weeks" (4 locales).
- Decided **against** an auto-release cron: the manual admin "Process guarantees" button is the human checkpoint before money moves. Revisit at higher volume.

## AI screening
- **Client-facing AI report**: separate prompt replaces the regex-masked internal report (migration 068, `2832f61`) — masked "—" sentences and collapsed tables are gone. → [[2026-07-11-client-facing-ai-report-prompt]]
- **Per-candidate AI presentation** replaces the batch Top-5 shortlist (`9e94aca`).
- AI eval no longer counts unanswered screening questions as gaps (`a26aa4f`).
- Company view: report moved behind a **View AI Report** button (`ad6c553`).

## Company surfaces
- Candidate view **rebuilt**: Kanban → filter-tab list; Presented/Paused folded away (`82c0edf`, `f643e5d`).
- Dashboard: **Active Candidates** card + real **Recent activity** feed from stage history (`1166f50`).
- Placement counts exclude failed placements everywhere + prod data repair (`6dc0fba`).

## Recruiter surfaces
- Dashboard: **Interview rate / Hire rate** boxes (`4f4d06d`).
- Mandate page: heading now "Ongoing process across all recruiters on this job" + grouped **rejection-reasons list** so recruiters see why the client rejects (`1c1450e`).

## Data integrity & infra
- **Stage-history completeness**: every candidate status path now writes `candidate_stage_history` (`6ece46a`, `d51c619`); prod-only `company_stage` columns captured in migration 066 (`7c184e5`).
- Admin job-detail 404 fixed — phantom `companies.linkedin_url` in a select (`93536f2`).
- Fresh-DB migration bootstrap made idempotent (`5a5f1b7`).
- Repo hygiene: untracked ~520 machine-state files (OMC replays, graphify cache, traces) + depth-proofed ignores (`98f0fec`).

Migrations applied to prod this week: **065, 066, 067, 068**.
