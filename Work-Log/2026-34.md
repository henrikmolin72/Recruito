# Week 34 (2026-08-17 → 2026-08-23)

## 2026-08-19 — Interview Date rename + recruiter dashboard fixes

Branch `feature/interview-date-dashboard-fixes` (subagent-driven execution of `docs/superpowers/plans/2026-08-19-interview-date-dashboard-fixes.md`, 4 annotated screenshots as spec).

- **Rename:** "Date of First Contact" → "Interview Date" across 4 dicts + company view + admin view + error copy (DB column unchanged).
- **Contact methods:** reduced to In Person / Video Call; stale-draft normalization; server-side allowlist at the shared `getMissingRequiredFields` trust boundary (TDD).
- **Future dates blocked:** native `max` on the date input + server bound; both derive from UTC (`toISOString`) so client is strictly stricter than server — no client-passes/server-rejects dead end; boundary "today accepted" pinned in tests.
- **Avg. time to hire root cause:** `updateCompanyStage` never stamped `hired_at` → always 0. Fixed via `statusChangeTimestampPatch` with a no-restamp guard (differential replay test); migration 073 backfills from stage history + refreshes snapshots. See [[2026-08-19-company-stage-timestamps-and-guarantee-rate]].
- **Guarantee rate:** counts `refund_processing` as failed (fn + app fallback aligned with `FAILED_PLACEMENT_STATUSES`); fn hardened with `SET search_path`.
- **Dashboard:** three stat sections merged into one `RecruiterOverview` card, 11 equal tiles (2/3/4-col grid), labels wrap (`break-words`), honest "—" fallbacks when the metrics row is missing.

Process: 3 implementer batches × (spec review + quality review), all approved; 417 vitest green (baseline 411), build + lint green. Prod steps pending: apply migration 073, diagnostic SQL for the demo recruiter's guarantee rows (stale-placement suspicion).

## 2026-08-20 — Production-readiness pass (security + code review, autonomous)

Branch `fix/prod-ready-security-2026-08-20` (commit `d408390`). Goal: do everything
doable without Henrik's secrets/prod-DB/legal input, verified green, with a security
check and a correctness code review.

**Verified baseline:** `main` is green — `npm run build` ✓, `npm run lint` ✓ (0 errors),
417/417 vitest. All feature work is merged (incl. EU AI Act truthing `66046e2`, contrary
to an earlier memory note that said it was unmerged).

**Fixed + verified (committed to branch, NOT merged/pushed):**
- **Next.js middleware-bypass CVE (HIGH, GHSA-6gpp-xcg3-4w24):** 16.2.9 → 16.3.1
  (fixed ≥16.2.11). App Router auth boundary. Blast radius was page shells only
  (admin layout + per-action re-auth + RLS are independent), but the gate itself
  was vulnerable. Accepted the 16.2→16.3 minor (adds a cosmetic `middleware`→`proxy`
  deprecation warning) to reach **0 npm vulnerabilities** — 16.2.12 alone left 3
  transitive highs (postcss source-map, sharp/libvips).
- **Schema leak (LOW, CLAUDE.md §6):** `updateCompanyProfile` returned the raw
  Postgres error; now a generic Swedish message + server-side log (mirrors
  `acceptCandidateProfileNotice`).
- Re-verified: build ✓, lint ✓ (0 errors), 417/417 vitest, `npm audit` 0.

**Security review (subagent):** 0 CRITICAL, 0 exploitable app-code holes. Auth guards,
IDOR ownership, magic-byte MIME, CSV escaping, rate limiting all present & consistent.
Only actionable finding was the Next CVE (fixed). Rate limiter fail-open on DB error is
a documented availability tradeoff (awareness only).

**Code review (subagent) — money-path findings NOT auto-fixed (need Henrik's decision;
`placements.ts` is load-bearing per §6, and Finding 1 embeds a money-semantics choice):**
1. [MEDIUM, CONFIRMED] Placement stuck in `payment_received` forever when a joining date
   is **backdated** past the guarantee window (payment-first ordering) — payout never
   releases. `placements.ts:334-346`.
2. [MEDIUM, CONFIRMED] Zero-month / already-elapsed guarantee: payment → `payout_released`
   never marks the candidate `completed` nor writes stage history (breaks the
   stage-history-completeness invariant). `placements.ts:162-217`.
3. [LOW] `recalculateRecruiterMetrics` is `requireAdmin()`-gated but called from the
   recruiter-only `updateCandidateStatus` → throws + swallowed every hire (self-heals via
   refresh-on-read). `candidates.ts:250-263`.
4. [LOW] Recruiter interview/hire-rate denominator (`v_submitted`) includes drafts;
   numerator excludes → rates skew low (+ latent >100% window). RPC in `073`.
5. [LOW] Avg-time-to-hire shows "0 days" not "—" with no hires (same no-data class the
   guarantee rate was fixed for). `performance-metrics.tsx:108`.
6. [LOW] Check-then-act on placement money transitions — no `.eq("status", …)` guard /
   affected-row check; concurrent fire could double-release. Low at current volume.

Pre-revenue → none bite today. Recommend fixing 1 & 2 (TDD, reproducing test first per §6)
before any real placement flows.

**Still needs Henrik (unchanged blockers):** RESEND_API_KEY in Vercel prod (email dead),
Anthropic credits (AI screening died once already), apply migration 073 + parity check
(see [Dev-Notes/prod-migration-073-and-parity.md](../Dev-Notes/prod-migration-073-and-parity.md)),
EU AI Act provider/deployer legal decision, Anthropic DPA/ZDR.

**Update (same day):** Findings 1 & 2 above FIXED with TDD (commit `1beb521`).
Reproducing tests red first, then minimal root-cause fixes — both enforce the
same invariant (reaching `payout_released` must complete the candidate + write
stage history). Bug 2: `recordPlacementPayment` payout_released branch now
completes the candidate. Bug 1: `setPlacementJoiningDate` releases the payout for
a `payment_received` placement whose backdated guarantee window has elapsed
(guarded to payment_received only — a confirmed/unpaid placement still needs
invoicing; pinned by test). 419/419 vitest (+2 repros), build + lint green.
Findings 3–6 (LOW) remain open. Branch now: `d408390` sec, `7157fe7` docs, `1beb521` money-path.
