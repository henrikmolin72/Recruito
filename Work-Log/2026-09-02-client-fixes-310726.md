# 2026-09-02 — Client fixes (images 310726)

Branch `fix/client-fixes-2026-09-02`. 13 client screenshots → 8 changes, driven as an ultragoal plan (`.omc/ultragoal/plans/1788331108180-*`).

## Changes
1. **Exclusive/Standard badge** on job detail — admin + company badge rows, recruiter earnings box (`job-preview-card.tsx`, company `jobs/[id]/page.tsx`; `assignmentExclusive/Standard` ×4 dicts).
2. **Admin Jobs list** — derived client-fee % under Original Fee (same style as recruiter %), and All/Active/Closed/Filled & Closed filter tabs (searchParams, same bucketing as company tabs).
3. **Missing workplace badge** root cause: `work_type` was skippable in wizard step 2 → NULL → no badge on recruiter cards. Now required (validation + error styling). Legacy NULL jobs have no data to show.
4. **Currency selector** — `CalculatorState.currency` is now `Currency | null`; new jobs start with NO currency and show a prominent button chooser ("Välj valuta för att fortsätta"); step 1 blocks until chosen; the in-calculator select was enlarged. Edit/draft flows keep their saved currency. Guarded the form's `recruitmentFee` useMemo (crashed on null during e2e — caught and fixed).
5. **Notification wording** — `adminJobFilledTitle` "A company filled a job" → "Candidate hired" (Kandidat anställd/ansatt/ansat).
6. **Admin Recruiters list** — new Active Mandates column from `job_mandates` + canonical `classifyMandate`; count expands to job titles via native `<details>`.
7. **Admin candidate view** — Message-company / Message-recruiter buttons → `/admin/messages/[candidateId]?party=…`; `getRecruitoThreadForAdmin` now returns an empty thread instead of 404 when no conversation exists yet.
8. **Messaging** — recruiter Messages page pins a "Kontakta Recruito" row (last-message preview) so admin replies are findable outside the bell; `sendSupportRequest` no longer puts the admin job link + Job ID into the recruiter-visible thread (email mirror to admins keeps full metadata).

## Verification
- `tsc` clean, `npm run lint` 0 errors (3 pre-existing warnings elsewhere), 524 tests green, `npm run build` green.
- Browser-e2e'd on local stack (all three roles): tabs+%, mandate expand, all three badges, wizard currency+work_type blocks, message buttons + first-message thread creation, pinned support row, stripped support metadata (old messages in thread still show the old format — historical data).

## Out of scope, flagged
- Pre-existing hydration error in `recruiter-inbox.tsx:75` (`toLocaleDateString` locale mismatch) — spawned as separate task chip.
